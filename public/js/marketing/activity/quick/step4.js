// 第四步
var step4 = function() {
    var $ = layui.$,
        setter = layui.setter,
        layer = layui.layer,
        form = layui.form;

    var storage = layui.data(setter.tableName);
    
    // 步骤初始化
    function onLoad() {

    }

    // 步骤重新显示
    function onShow() {

    }

    return {
        onLoad: onLoad,
        onShow: onShow,
    };
};